{{- if .Values.gateway.enabled }}
The site will be available at https://{{ .Values.gateway.host }}/ after DNS and certificate reconciliation.
{{- else }}
Gateway resources are disabled. Port-forward the service to inspect the site:

  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "leonid-sh.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}
